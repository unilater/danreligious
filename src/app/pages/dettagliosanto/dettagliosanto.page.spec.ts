import { ComponentFixture, TestBed } from '@angular/core/testing';
import { DettagliosantoPage } from './dettagliosanto.page';

describe('DettagliosantoPage', () => {
  let component: DettagliosantoPage;
  let fixture: ComponentFixture<DettagliosantoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(DettagliosantoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
